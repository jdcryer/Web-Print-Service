const expect = require("chai").expect;
const label = require("../build/modules/label_constructor");

describe("test", () => {
        it("testit", () => {
                expect(1).to.equal(1);
        })
});
